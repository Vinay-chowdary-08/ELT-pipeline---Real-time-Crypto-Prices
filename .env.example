# API Configuration (CoinGecko free tier: leave empty for basic use)
# Get a key at https://www.coingecko.com/en/api/pricing for higher rate limits
COINGECKO_API_KEY=

# Data paths (relative to project root)
DATA_DIR=data
DB_PATH=data/crypto_prices.duckdb

# Optional: Ingest configuration
# COINS=bitcoin,ethereum,solana
# CURRENCY=usd
